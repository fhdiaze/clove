#include <stdio.h>
#include <stdlib.h>

int main(void) {
    unsigned uint = -4;
    signed sint = -4;

    printf("unsigned=%u\n", uint);
    printf("signed=%d\n", sint);

    return EXIT_SUCCESS;
}
